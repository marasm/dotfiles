## Tool configuration repo

### Instructions
Follow the below to set up:
 - Clone this repo to ~/dotfiles 
 - Make sure `stow` is installed 
 - cd into the dotfiles repo and use the following command for each module you need
    ```
    stow <module name>
    ```
